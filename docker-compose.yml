services:
  mariadb:
    image: mariadb:11.4.8-ubi9
    container_name: murmellib_db
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD}
      MARIADB_DATABASE: ${MARIADB_DATABASE}
      MARIADB_USER: ${MARIADB_USER}
      MARIADB_PASSWORD: ${MARIADB_PASSWORD}
    ports:
      - "${MARIADB_HOST}:${MARIADB_PORT:-3306}:3306"
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: [ "CMD-SHELL", "mariadb-admin ping -h ${MARIADB_HOST} -uroot -p${MARIADB_ROOT_PASSWORD} ||exit 1" ]
      interval: 5s
      timeout: 5s
      retries: 3

volumes:
  db_data: